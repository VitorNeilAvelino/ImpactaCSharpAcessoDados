<Window x:Class="CSharp2.Capitulo10.Wpf.Produtos.PedidosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pedidos" Height="433" Width="854.531"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="Black" Offset="0"/>
                <GradientStop Color="#FF8B8B8B" Offset="0.48"/>
                <GradientStop Color="Black" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <GroupBox Header="Pedido" VerticalAlignment="Top" Margin="10,0" Padding="10">
            <StackPanel VerticalAlignment="Top" Orientation="Horizontal">
                <Label Content="Vendedor" VerticalAlignment="Center"/>
                <ComboBox x:Name="vendedorComboBox" Width="200" ItemsSource="{Binding Vendedores}" DisplayMemberPath="Pessoa.Nome" SelectedValuePath="Id"/>
                <Label Content="CPF do Cliente" VerticalAlignment="Center"/>
                <TextBox x:Name="cpfCliente" Height="23" TextWrapping="Wrap" Text="" Width="100" ToolTip="CPF do cliente" />
                <Label x:Name="nomeCliente" Content="" Width="200" Margin="10,0,0,0"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>